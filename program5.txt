#include <stdio.h>
#include <string.h>

int main()
{
    int l;
    char str[25];

    printf("Enter String: ");
    fgets(str, sizeof(str), stdin);

    l = strlen(str);

    printf("Length of the string is: %d", l);

    return 0;
}